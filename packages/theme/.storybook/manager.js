import { addons } from "@storybook/addons";
import { themes } from "@storybook/theming";

addons.setConfig({
  theme: themes.light,
  brandTitle: "LogChimp",
  brandUrl: "https://logchimp.codecarrot.net/",
  brandImage: "https://retm.app/app-logo-icon.png",
});
